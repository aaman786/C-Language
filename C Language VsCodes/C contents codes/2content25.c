//for finding the length of string
#include <stdio.h>
#include <string.h>
int main()
{
    char a[] = "Programming";
    int length_a;
    length_a = strlen(a);
    printf("The length of a is: %d", length_a);
    return 0;
}